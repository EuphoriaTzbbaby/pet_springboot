server:
  port: 7492
  servlet:
    context-path: /tzb
spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:  jdbc:mysql://47.101.42.177:3306/pet?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Tzb304213
    hikari:
      max-lifetime: 1200000         # 30分钟，建议小于 MySQL 的 wait_timeout（默认8小时）
      keepalive-time: 300000  # 5分钟发送一次心跳
      idle-timeout: 600000          # 10分钟
      connection-timeout: 300000     # 30秒，连接超时设置合理
      validation-timeout: 100000  # 连接校验超时时间（毫秒）66688
      leak-detection-threshold: 50000  # 检测连接泄漏（毫秒）
  #    type: com.alibaba.druid.pool.DruidDataSource
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
mybatis:
  type-aliases-package: tzb.pojo
  configuration:
    map-underscore-to-camel-case: true
#  mapper-locations: classpath:mappers/*.xml



logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: trace

oss:
  endpoint: ${OSS_ENDPOINT}
  accessKeyId: ${OSS_ACCESSKEYID}
  accessKeySecret: ${OSS_ACCESSKEYSECRET}
  bucketName: ${OSS_BUCKETNAME}


