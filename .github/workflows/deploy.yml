name: Deploy Spring Boot Application

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Build Maven package (skip tests)
        run: mvn clean package -DskipTests

      - name: Upload JAR to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: 47.101.42.177
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: target/pet-0.0.1-SNAPSHOT.jar
          target: /var/www/html/springbootAndVue/pet
          strip_components: 1  # ✅ 就是这句去掉 target 文件夹 fuck you

      - name: Build and run Docker container on server
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: 47.101.42.177
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd /var/www/html/springbootAndVue/pet
            docker build -t sb-pet .
            docker stop spring-pet || true
            docker rm spring-pet || true
            docker run -d --name spring-pet -p 7492:7492 --network blog sb-pet